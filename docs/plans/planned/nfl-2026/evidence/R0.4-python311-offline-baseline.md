# R0.4 Python 3.11 Offline Baseline

Date: 2026-09-06
Packet: R0.4
Baseline commit: `db1785fdfea8a2f3b972fdc5bfd7a8990b3b08fe`
Temporary branch: `codex/r0-4-baseline`

## Scope and provenance

This packet was run in a separate temporary worktree created directly from
the locally available `origin/main` commit above. The source checkout was
dirty with uncommitted QB work; it was not copied, reset, or modified by this
packet. The worktree was clean before the evidence document was added, and no
source, data, notebooks, models, or betslips files were changed.

The configured remote could not be refreshed. Exact command and result:

```text
git fetch --dry-run origin main
fatal: unable to access 'https://github.com/b00se/sportsbalf.git/': Failed to connect to 127.0.0.1 port 9: Connection refused
fetch_exit=128
```

Therefore this baseline is explicitly against the existing local
`origin/main` reference, not a newly fetched remote state.

## Python 3.11 executable evidence

The system launcher reports Python 3.11.3 at `C:\Python311\python.exe`, but
that installation does not contain pytest. The repository environment was
used reproducibly by absolute path:

```text
C:\Users\BOOSE\sportsbalf\.venv\Scripts\python.exe --version
Python 3.11.3
C:\Users\BOOSE\sportsbalf\.venv\Scripts\python.exe -m pytest --version
pytest 8.4.2
C:\Users\BOOSE\sportsbalf\.venv\Scripts\ruff.exe --version
ruff 0.6.9
```

No package installation or network dependency resolution was performed.

## Offline test/lint baseline

The exact test command was run from this worktree with plugin autoloading and
package/network resolution disabled:

```powershell
$env:PYTEST_DISABLE_PLUGIN_AUTOLOAD='1'
$env:PIP_NO_INDEX='1'
$env:UV_NO_NETWORK='1'
& 'C:\Users\BOOSE\sportsbalf\.venv\Scripts\python.exe' -m pytest -q
```

Runtime: 22.18 seconds. Exit code: 1. Result: 362 passed, 3 failed, 80
warnings. The three failures are all in existing MLB model-selection tests:

```text
tests/test_mlb_model_selection.py::test_registry_candidates_fit_and_predict
tests/test_mlb_model_selection.py::test_pipeline_champion_model_roundtrip_predicts_with_strategy
tests/test_mlb_pitcher_prop_model_selection.py::test_pitcher_props_model_selection_roundtrip_predicts_with_strategy
```

The first failure is a Windows `[WinError 5] Access is denied` creating a
joblib thread pool. The other two fall back to the baseline xgboost model
after that same tournament failure and then fail their model-name assertions.
These are pristine-upstream baseline failures: this worktree is exactly the
local `origin/main` commit, and `git diff` against that commit was empty before
this evidence file was created. No NFL/QB failure occurred.

The exact lint command was:

```powershell
& 'C:\Users\BOOSE\sportsbalf\.venv\Scripts\ruff.exe' check .
```

Runtime: included in the same command invocation after pytest (under one
second). Exit code: 0. Result: `All checks passed!`

## Reproduction instructions

From a fresh checkout with the local `origin/main` reference available:

```powershell
git worktree add -b codex/r0-4-baseline <temporary-worktree> db1785fdfea8a2f3b972fdc5bfd7a8990b3b08fe
Set-Location <temporary-worktree>
$env:PYTEST_DISABLE_PLUGIN_AUTOLOAD='1'
$env:PIP_NO_INDEX='1'
$env:UV_NO_NETWORK='1'
& 'C:\Users\BOOSE\sportsbalf\.venv\Scripts\python.exe' -m pytest -q
& 'C:\Users\BOOSE\sportsbalf\.venv\Scripts\ruff.exe' check .
```

Expected baseline: pytest exits 1 with 362 passed and 3 known upstream MLB
model-selection failures; Ruff exits 0. The R0.4 gate records this baseline
only. No R1 implementation was started.
