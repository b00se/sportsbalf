# R0 Closeout and R1 Handoff

Date: 2026-09-06
Durable R0 record: `codex/nfl-2026-implementation`

## Durable references

| Reference | Commit | Purpose |
|---|---|---|
| `codex/nfl-2026-implementation` | `1aad939ca620d8edc818cdf61c9e8af421483209` | Integrated R0 evidence, Python tooling, and quality cleanup; pushed to `origin`. |
| `refs/recovery/r0.1-qb` | `444c249d5250609dd363231bf64efce63a679086` | Authoritative raw-byte preservation of the pre-R0 dirty QB work. Do not alter or replay it. |
| `codex/r0-1-qb-recovery` | `444c249d5250609dd363231bf64efce63a679086` | Named local branch for visible recovery access; points exactly to the recovery ref. |
| `codex/r0-3-salvage-original` | `26800898d41096a2f65272e133892dfbe6a9b23f` | Original synthetic-parent R0.3 ledger commit, retained for provenance. |
| `codex/r0-integration-review` | `1cc0f91c829a726a7a3d37801d40474e2147de53` | Pre-tooling R0 integration evidence checkpoint. |
| `codex/r0-4-baseline` | `c96cbcd55367e7851bff7088175d074ac231b1cd` | Original R0.4 baseline evidence checkpoint. |

The authoritative recovery ref and its named branch contain identical raw
bytes for all eight recovered files, including
`instructions/qb_attempts_phase3.md`. The instruction's reusable provider,
temporal-safety, staleness, uncertainty, and backtest acceptance intent is
explicitly retained by the R0.3 ledger and mapped to R2.1, R2.3, R2.5, R3.2,
R3.6, and R3.8. Its legacy delivery surfaces are deferred or rejected; no QB
code is to be ported verbatim into R1.

## Consolidated evidence

The durable R0 record contains these evidence files without rewriting their
original findings:

- `R0.1-recovery.md`
- `R0.3-salvage-ledger.md`
- `R0.4-python311-offline-baseline.md`
- `R0.tooling-modernization.md`

## Temporary-artifact audit

| Path | Audit result | Closeout action |
|---|---|---|
| `.r0-3-salvage-clone` | Clean clone plus three transfer bundles. Its only clone-local source commit is retained as `codex/r0-3-salvage-original`; the integrated ledger is already present on the durable branch. | Remove after this record is committed. |
| `.r0-4-worktree` | Clean linked worktree at `c96cbcd`; its evidence is on the durable branch and its branch is retained. | Remove after this record is committed. |
| `.r0-tooling-clone` | Clean detached checkout at `1aad939` plus two bundles; all contained work is pushed on `origin/codex/nfl-2026-implementation`. | Remove after this record is committed. |

The transfer and quality bundles were inspected before removal and contain no
unrepresented R0 work. The transfer bundle's `8e59086` is a
trailing-whitespace-only predecessor of retained `1cc0f91`, with the same
substantive evidence patch. No branch, ref, or recovery object is deleted by
this closeout.

## R1 handoff

Start R1 from `codex/nfl-2026-implementation` after confirming the upstream
state required for that fresh wave. R0 is closed; do not begin R1 work in this
closeout.

## Final clean-root verification and caveat

On the clean root checkout at this closeout commit, `uv sync --locked` was run
once to populate missing locked wheels. The following offline checks then
reproduced successfully: `uv lock --check --offline --no-config`, Ruff lint,
Ruff format (223 files), and `uv run --offline --no-config pytest -q` (365
passed, 161 warnings).

The exact locked Pyright version is `1.1.411`, which reports 2,514 diagnostics
in the clean root. The earlier `0 errors, 0 warnings` record came from an
isolated temporary environment that was not demonstrated to be synchronized
to this lock. It is therefore not a reproducible clean-type baseline. This is
a documented R0 tooling caveat for a later dedicated quality packet; do not
mask it, weaken type rules, or treat it as R1 implementation work.
