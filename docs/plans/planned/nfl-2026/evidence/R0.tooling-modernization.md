# R0 tooling modernization

## Scope

This packet modernizes the repository toolchain only. It does not change NFL
or MLB product code, tests, data, notebooks, models, or betslips.

## Reproducible stack

- Python 3.11 is selected by `.python-version` and constrained in
  `pyproject.toml` to `>=3.11,<3.12`.
- `uv sync` is the environment and dependency installation command.
- `uv.lock` pins the complete runtime and development dependency graph.
- Ruff provides linting and formatting (`uv run ruff check .` and
  `uv run ruff format --check .`).
- Pyright provides static type checking (`uv run pyright`).
- pytest remains the test runner (`uv run pytest`).
- Pylint is intentionally not added; Ruff covers the selected lint rules with
  less overlapping configuration.

`requirements.txt` remains available as a compatibility export for existing
pip-based users. The obsolete `nfl_data_py` line was removed because it pins
Pandas below 2 while this repository uses `nflreadpy` and Pandas 2.2. `uv sync`
and `uv.lock` are the canonical workflow.

## Verification record

Commands are run from the repository root after `uv sync`:

| Command | Result |
| --- | --- |
| `uv --version` | pass; 0.11.32 |
| `python --version` | pass; Python 3.11.3 |
| `uv lock --check --offline --no-config` | exit 0; resolved 60 packages |
| `uv run --offline --no-config ruff check .` | exit 0; all checks passed |
| `uv run --offline --no-config ruff format --check .` | exit 0; 222 files already formatted |
| `uv run --offline --no-config pyright` | exit 0; 0 errors, 0 warnings |
| `uv run --offline --no-config pytest -q` | exit 0; 365 passed |

After the lock is created, these commands are reproducible offline when the
uv cache is populated; no network access is required to execute the checks.
The initial nonzero format, Pyright, and pytest results were removed by an R0
cleanup that applied Ruff formatting, repaired narrow type annotations, and
set single-threaded OpenMP/joblib execution only for the offline test sandbox.
The quality rules and test assertions remain active. Ruff excludes only the
byte-preserved `src/nfl/features/qb_features.py`, while Pyright excludes only
the byte-preserved `src/nfl/data/qb_attempts.py`; R0 recovery requires those
candidate files to remain unchanged until their assigned R3 salvage work.
