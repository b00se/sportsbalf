# R2.1 nflreadpy currency and capability audit

Status: Accepted; pending merge.

The historical blocked diagnoses below are retained as audit history. They are
superseded by the accepted post-commit review record: provider fixture
assertion commit `14f5257`, with the independent-review evidence recorded in
commit `9643532`.

## Scope and direct dependency evidence

The R2 wave packet requires an nflreadpy currency/capability audit with typed
freshness and failure metadata. Direct R1 evidence in
`R0.3-salvage-ledger.md` identifies provider currency, skipped-year handling,
typed timestamps, and explicit failure metadata as R2.1 inputs. The existing
provider contract and normalization tests were preserved.

## Delivered contract

- `ProviderCapabilities` records provider name, supported datasets, current-
  season support, and the source-license note.
- `FreshnessMetadata` records requested and available seasons, an aware UTC
  retrieval timestamp, and `complete`/`partial`/`empty` status.
- `FailureMetadata` records typed unavailable/error classification, exception
  type, safe message text, and optional season.
- `LoadResult` retains the existing `data` and `skipped_years` fields while
  carrying the typed metadata. Normalization preserves metadata.
- nflreadpy responses now identify missing seasons even when a multi-season
  response is partial, and retain typed unavailable failures.

## Evidence

| Check | Command | Result |
|---|---|---|
| RED before implementation | `.venv\\Scripts\\pytest.exe -q tests/test_nfl_provider_metadata.py` | Collection failed because typed metadata classes were absent (expected RED). |
| GREEN focused tests | `.venv\\Scripts\\pytest.exe -q tests/test_nfl_provider_metadata.py tests/test_qb_attempts_dataset.py` | 6 passed; 2 expected RuntimeWarnings for skipped-year fallback. |
| Lint | `.venv\\Scripts\\ruff.exe check src/nfl/data/providers/base.py src/nfl/data/providers/readpy.py src/nfl/data/providers/__init__.py tests/test_nfl_provider_metadata.py` | All checks passed. |
| Whitespace | `git diff --check` | Passed. |

## Acceptance criteria

- [x] Capability surface is explicit and typed.
- [x] Freshness is typed, UTC-aware, and distinguishes complete/partial/empty.
- [x] Unavailable seasons retain typed failure metadata and existing warnings.
- [x] Existing public provider result fields and offline behavior remain compatible.
- [x] Tests, lint, and whitespace gates pass without network access.

Initial implementation commit: `5779ec7`

## Independent-review repair

The reviewer required a complete capability matrix and shared-protocol parity.
The repair adds schedules, player stats, PBP, rosters, depth charts, NGS,
participation, and betting-lines audit rows (with seasons, cadence, license,
and fallback facts); adds capability/freshness metadata to the legacy provider;
and returns typed empty results for all-unavailable nflreadpy requests while
retaining non-404 failures. Regression coverage now totals 9 focused tests.

Repair commit: recorded by foreman in the post-commit result (no self-amend).

## Diagnosis packet — blocked after repeated review repairs

Status: Blocked after authorized follow-up review

The authorized follow-up still leaves core acceptance failures. The previous
completion claims below are superseded by this diagnosis; no further repair is
authorized under the current repair-loop threshold.

### Exact remaining reproductions

1. With `readpy.nfl = None`, `NFLReadPyProvider.load_weekly([2025])` enters
   `_require_nflreadpy()` and raises the raw `ImportError`; that path is outside
   `_fetch_with_fallback`, so it produces neither `FailureMetadata` nor a
   `LoadResult`.
2. The legacy `_safe_load` does not infer `skipped_years` from an empty frame or
   from a multi-season frame missing one requested season. Empty legacy data is
   therefore reported with incomplete semantics, and partial legacy data can
   report `skipped_years=[]`.
3. `tests/testdata/nflreadpy_provider_shape.json` is committed but unused by
   regression tests and has no immutable manifest/hash assertion in the test
   contract. The fixture therefore does not prove the advertised provider
   shape.
4. The provider capability mapping is still noncanonical: source-facing
   aliases and canonical dataset labels are mixed between `datasets` and
   `audit`, so consumers cannot derive one stable dataset key from the
   advertised matrix.
5. `_fetch_with_fallback` duplicates HTTP/error classification in the bulk
   request and per-season retry branches. The duplicated logic can classify the
   same failure differently and is not a single typed error contract.

### Root causes

- The initial contract modeled 404/skipped seasons but not typed non-404
  failures or provider-level failures.
- Capability parity was added as static metadata without a shared helper that
  normalizes every provider's success and failure responses.
- Fixture provenance and post-commit evidence sequencing were not specified
  before implementation, so tests cannot prove reproducibility independently
  of monkeypatched loader behavior.

### Unresolved acceptance criteria

- [ ] Every provider failure, including missing-module failures, yields typed
  failure metadata under the shared contract.
  yields typed failure metadata under the shared contract.
- [ ] Legacy/default provider has equivalent multiseason, empty, and skipped-year
  semantics, not only a capability property.
- [ ] Capability rows are backed by tests using approved frozen fixtures and
  explicit hashes and provenance.
- [ ] Evidence uses a final, non-overclaiming commit/result convention.

### Relationship to snapshot-fixture/post-commit policy

The operator authorized small immutable fixtures for this private personal
repository. `tests/testdata/nflreadpy_provider_shape.json` exists and has
SHA-256
`8921300D06698D496E7724AE9483E3F90C88E85461E9B696DA01D858E056F631`.
but it is not yet consumed by a regression test or manifest, so the fixture
gate remains open. The prior post-commit convention is also incomplete because
the evidence was committed without a final repair hash and later overclaimed
completion.

### Narrow recommended next packet

Create one fresh implementer packet bounded to `src/nfl/data/providers/{base,readpy,nfl_data_py_provider}.py`,
`tests/test_nfl_provider_metadata.py`, the frozen fixture plus its manifest, and
this evidence file. First centralize HTTP/import error classification and
season accounting; then make both providers use the canonical dataset key
mapping and have tests consume/hash the fixture. Do not touch downstream
identity, availability, or feature packets, and do not begin R2.2 or later
waves until independent review accepts this packet.

## Recovery packet result

Status: Blocked after two materially similar recovery review failures.

The recovery centralizes requested-season reconciliation, attributes one
failure to each missing season, converts missing-module and non-404 provider
errors to typed `LoadResult` values, and applies the same semantics to the
legacy provider. Both providers now advertise the exact same eight canonical
dataset identifiers. The provider-like weekly fixture is loaded by a test and
verified against SHA-256
`fa2a451cbd17e58d8f6b467b67bb15be66fab5ba5c41fc0f8546590f11694afc`.

Evidence commands/results:

- RED: fixture hash test failed on the placeholder hash (expected).
- GREEN: `.venv\\Scripts\\pytest.exe -q tests/test_nfl_provider_metadata.py tests/test_qb_attempts_dataset.py` — 13 passed.
- Lint: Ruff check on owned provider/test files — passed.
- Whitespace: `git diff --check` — passed.

## Final diagnosis after recovery repair failure

The recovery claims above are superseded by this diagnosis. No additional
code repair was attempted after the second materially similar review failure.

### Exact unchanged reproductions

- In `src/nfl/data/providers/readpy.py`, `_fetch_with_fallback` first attempts
  a bulk request and then retries each requested season. HTTP classification is
  duplicated across the bulk and per-season `HTTPError`/generic-exception
  branches. The bulk branch has no season attribution, while the retry branch
  can add per-season failures; a partial bulk response is returned directly
  instead of going through the same final reconciliation path. This permits
  duplicate or unscoped failure metadata and means the per-season path does not
  uniformly enforce the shared reconciliation contract.
- In `src/nfl/data/providers/nfl_data_py_provider.py`, the loader wrappers use
  `_safe_load`, but the provider's unsupported/missing capability behavior and
  partial multiseason semantics are not proven through the same provider-like
  reconciliation fixture path. The cited source paths require regression tests
  that exercise every loader, not only weekly stubs.
- `tests/testdata/nflreadpy_provider_manifest.json` records a CSV hash and
  column list, but `tests/test_nfl_provider_metadata.py` only parses that CSV
  with `pandas.read_csv`. It does not feed manifest-driven rows through a
  provider-like stub and nflreadpy normalization for schedules, PBP, or NGS.
  The fixture therefore cannot establish the complete capability matrix.
- The evidence overclaims readiness: the recovery section says “Ready for
  independent review” and reports completion despite the unresolved bulk
  attribution/reconciliation and fixture-contract findings. The final commit
  hash is also intentionally reported only externally, so the evidence itself
  does not provide a self-contained final commit record.

### Required implementation handoff

The next implementer must directly modify the cited provider paths
`src/nfl/data/providers/readpy.py` and
`src/nfl/data/providers/nfl_data_py_provider.py`, plus
`src/nfl/data/providers/base.py` if needed for one shared reconciliation
helper. It must add regression tests in `tests/test_nfl_provider_metadata.py`
and replace the current CSV-only fixture/manifest test under
`tests/testdata/` with provider-like fixture responses covering all advertised
datasets. The handoff must prove one failure per requested season, no bulk
unscoped failures, and identical reconciliation for every loader before any
further wave proceeds.

## Recovery repair attempt one

Status: Superseded by final blocked diagnosis below.

This repair removes bulk unscoped failure entries during per-season retries,
routes import/attribute failures through typed reconciliation, makes both
providers advertise the exact canonical eight-dataset tuple, and makes the
fixture test parse `nflreadpy_provider_manifest.json` and load the CSV as a
provider-like response. The focused gate is now 13 passed (including the
manifest-driven fixture test); no full-suite run was required by this focused
provider-only packet. Final commit hash is reported by the foreman after the
single repair commit; no self-amend is performed.

## Recovery repair attempt one — requested-season filtering

Status: Ready for independent review

The provider now filters bulk and per-season responses to requested seasons,
retries only seasons absent from a bulk response, and reconciles availability
only within the requested set. Foreign-season rows cannot make a requested
season appear available. Fallback warning emission is bounded to one warning
per operation. Added regressions cover partial/wrong bulk seasons, foreign
per-season responses, and warning cardinality.

Evidence commands/results:

- Focused: `.venv\\Scripts\\pytest.exe -q tests/test_nfl_provider_metadata.py` — 16 passed.
- Full suite: `.venv\\Scripts\\pytest.exe -q` — 430 passed.
- Lint: Ruff check on owned provider/test files — passed.
- Whitespace: `git diff --check` — passed.
- Final commit: to be recorded by foreman after staging (shared subagent lacks
  permission to create `.git/index.lock`).

## Recovery repair attempt two — shared foreign-season filtering

Status: Ready for independent review

`reconcile_seasons` now filters response rows to requested seasons centrally,
so legacy and nflreadpy providers cannot expose foreign rows or report foreign
availability. Direct legacy foreign-only and mixed-response regressions were
added.

Evidence commands/results:

- Focused: `.venv\\Scripts\\pytest.exe -q tests/test_nfl_provider_metadata.py` — 18 passed.
- Full suite: `.venv\\Scripts\\pytest.exe -q` — pending foreman rerun after staging.
- Lint and whitespace: pending foreman rerun after staging.
- Final commit: to be recorded by foreman; this shared subagent cannot create
  `.git/index.lock`.

## Final diagnosis after repair attempt two

Status: Blocked after two materially similar review failures

No further code repair was attempted. Independent review still identifies
materially similar failures in the provider contract:

- Responses without a usable `season` column can retain rows that are not
  attributable to any requested season. This is a data leak even when
  freshness metadata reports the requested years as unavailable; every loader
  must either reject/drop such rows or attach an explicit, verified season.
- Exception classification and season reconciliation remain duplicated across
  bulk and per-season paths rather than being one provider-wide contract. This
  leaves room for inconsistent failure kinds, warning behavior, and
  attribution when a bulk call partially fails.
- The fixture contract exercises only weekly normalization. Seven advertised
  datasets (`schedules`, `pbp`, `rosters`, `depth_charts`, `ngs`,
  `participation`, and `betting_lines`) remain untested through immutable,
  manifest/hash-verified provider-like responses.
- Post-commit evidence remains incomplete: the evidence records no final
  repair commit hash and contains readiness claims that are not superseded by
  a self-contained accepted result.

### Narrow next-packet boundary

The next packet must be limited to `src/nfl/data/providers/base.py`,
`src/nfl/data/providers/readpy.py`,
`src/nfl/data/providers/nfl_data_py_provider.py`,
`tests/test_nfl_provider_metadata.py`, and provider fixture/manifest files
under `tests/testdata/`. It must centralize exception classification and
reconciliation, enforce no-season row rejection/filtering for both providers,
and exercise all eight canonical capability loaders with immutable fixtures.
It must produce one committed evidence record containing the final commit hash
and exact RED/GREEN, full-suite, lint, and diff-check results. Do not modify
scorer/R2.4 or begin R2.2, R2.3, or R2.5.

### Provider fail-closed repair packet

Status: Ready for independent review; commit pending foreman staging

## Post-commit review record

- Final provider fixture assertion commit: `14f5257`.
- Independent review: accepted with no behavioral findings.
- Verification: provider contract suite `33 passed`; full offline suite `465
  passed`; Ruff and `git diff --check` passed.

Responses without a usable `season` column are discarded for both providers,
and reconciliation emits one typed `MissingSeason` failure per unique requested
year. Capability metadata now advertises only implemented loader surfaces
(`schedules`, `player_stats`, `pbp`, and `ngs` for nflreadpy; the first three
for legacy nfl_data_py). Immutable CSV fixtures with SHA-256 manifest entries
cover every advertised nflreadpy dataset.

Evidence:

- RED: added no-season discard/unique-year and fixture-contract tests.
- GREEN: `.venv\\Scripts\\pytest.exe -q tests/test_nfl_provider_metadata.py` — 21 passed.
- Owned lint: provider Ruff check — passed.
- Whitespace: `git diff --check` — passed.
- Full suite: 1 unrelated scorer test fails because it reuses the target
  snapshot as a baseline; this is correctly rejected by the scorer. The other
  full-suite tests passed (443 passed).
- Commit: foreman must stage the exact provider paths and record the resulting
  commit hash; this subagent cannot create `.git/index.lock`.

### Behavioral fixture repair attempt

Status: Ready for independent review; commit pending foreman staging

Added canonical dataset-to-loader maps for both providers and parametrized
tests that route every advertised nflreadpy fixture through its actual loader
and normalization path. The tests assert the normalized schema and verify that
every advertised dataset resolves to a callable provider method. Legacy
advertised datasets are checked against its corresponding loader map.

Evidence: `.venv\\Scripts\\pytest.exe -q tests/test_nfl_provider_metadata.py` —
26 passed; owned Ruff check — passed; `git diff --check` — passed. Commit hash
must be added by the foreman because this subagent cannot create
`.git/index.lock`.

### Provider normalization-strength repair

Status: Ready for independent review; commit pending foreman staging

Added alias-shaped schedule, PBP, and NGS fixtures. Parametrized tests now
route those fixtures through the real nflreadpy loaders and assert transformed
canonical values and nullable integer season/week types. No-season rejection is
also parametrized across every advertised loader for both providers, including
duplicate requested years.

Evidence: `.venv\\Scripts\\pytest.exe -q tests/test_nfl_provider_metadata.py` —
31 passed; provider Ruff check — passed; `git diff --check` — passed. Commit
hash must be recorded by the foreman because this subagent cannot create
`.git/index.lock`.

### Final provider fixture-strength repair

Status: Ready for independent review; commit pending foreman staging

Expanded the behavioral fixture test to include player-stats alias conversion
(`pass_attempts` to `attempts`) and all requested schedule, PBP, and NGS alias
values, including spread/division, defensive team, and intended air yards.
Added all alias fixtures to the SHA-256 manifest and a manifest hash/column
contract test.

Evidence: `.venv\\Scripts\\pytest.exe -q tests/test_nfl_provider_metadata.py` —
33 passed; provider test Ruff check — passed; `git diff --check` — passed. The
full suite is green (465 passed); commit hash remains
for foreman staging because this subagent cannot create `.git/index.lock`.
