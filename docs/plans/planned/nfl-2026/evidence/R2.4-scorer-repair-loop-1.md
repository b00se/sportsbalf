# R2.4 scorer repair loop 1

Status: Implemented; independent review pending.

This packet hardens source lineage and baseline separation. URL parsing now
fails closed for malformed hosts, empty/interior-dot labels, leading/trailing
hyphens, malformed bracket hosts, and non-HTTP(S) URLs. Directly constructed
metadata with a missing/non-string source family produces typed audit failure.
Scoring rejects omitted paths, target/public and target/consensus aliases, and
public/consensus aliases. Baseline roles also require distinct normalized
families and registrable domains.

Evidence:

- RED: focused scorer regressions failed before these changes.
- GREEN: `.venv\\Scripts\\pytest.exe -q tests/fantasy/adapters/nfl/test_projection_sources.py` — 30 passed.
- Full suite: `.venv\\Scripts\\pytest.exe -q` — 457 passed.
- Lint: `.venv\\Scripts\\ruff.exe check src/fantasy/adapters/nfl/projection_sources.py tests/fantasy/adapters/nfl/test_projection_sources.py` — passed.
- Diff check: `git diff --check` on owned scorer paths — passed.

Post-commit convention: foreman should commit only the owned source, test,
and this evidence file, then append the resulting hash and independent
Luna-high review result. No provider or downstream files are in scope.
