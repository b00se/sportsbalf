# R3.1 Evidence

Status: complete
Implementation commit: `661e12d` (superseded by the amended commit below)
Review result: independent Luna-high review found two P1 leakage defects;
both repairs were applied and revalidated before the final amended commit.
Dependencies: R2 merged in PR #23 (`a8a3e37`)

## Changed files

- `src/nfl/models/team_projections.py`
- `tests/test_nfl_team_projections.py`
- `docs/plans/planned/nfl-2026/evidence/R3.1-team-projections.md`

## Commands and exit codes

- RED: `.venv\\Scripts\\pytest.exe -q tests/test_nfl_team_projections.py` — exit 2 (module did not yet exist).
- GREEN (initial): `.venv\\Scripts\\pytest.exe -q tests/test_nfl_team_projections.py` — exit 0; 4 passed.
- GREEN (after independent-review repairs): `.venv\\Scripts\\pytest.exe -q tests/test_nfl_team_projections.py` — exit 0; 6 passed.
- `.venv\\Scripts\\ruff.exe check src/nfl/models/team_projections.py tests/test_nfl_team_projections.py` — exit 0.
- `git diff --check` — exit 0.

## Review findings and repairs

- P1 timezone cutoff leakage: aware cutoffs now convert to UTC before the
  normalized UTC-naive history comparison; regression covers a `+05:00`
  cutoff whose UTC instant precedes a row with an earlier wall-clock hour.
- P1 same-date rolling-origin leakage: duplicate team dates now fail closed,
  and each origin asserts all training dates are strictly earlier than its
  test cutoff; regression covers duplicate-date observations.

## Exit-criteria mapping

1. Typed deterministic offline-safe team contract: `TeamProjection` uses a trailing arithmetic mean over supplied historical rows only.
2. Reconciled volume: projected total plays is exactly projected pass volume plus projected rush volume.
3. Bounded opportunities: projected scoring opportunities are nonnegative and clipped to projected total plays.
4. As-of safety: rows at or after the supplied cutoff are excluded by strict timestamp filtering.
5. Transparent baseline and rolling-origin comparison: `evaluate_rolling_origin` compares trailing mean with an expanding-mean baseline, and promotes only when model MAE ties or beats baseline MAE. The focused fixture demonstrates promotion.

## Deferred risks

The component contract intentionally uses a transparent trailing mean until later R3 packets supply richer team/context features.

## Newly unblocked tasks

None. Do not begin the next wave.

## Final commit

The final task commit is the commit that contains this reviewed evidence record;
its hash is verified from the repository log during wave integration.
