# R3.13-B Real direct-FP paired tournament

Status: Evidence captured; direct-FP weekly result is conclusive tie-or-better
against the internal baseline but remains unpromoted. R4 blocked.

## Scope and authoritative inputs

This packet records the direct-fantasy-point paired tournament run against the
authoritative archived outcome artifact produced by the real NFLverse cache-only
run on 2026-09-11. The tournament compared the direct-FP candidate with the
strictly prior `player_trailing_mean` internal baseline on identical canonical
player-week keys. Fold deltas are `direct MAE - player-trailing-mean MAE`.

The immutable archive and result bundle are linked by the baseline run manifest:

- Archive cache: `C:\\Temp\\sportsbalf-nfl-r3-20260911`
- Archive payload: `C:\\Temp\\sportsbalf-nfl-r3-20260911\\nflverse_weekly.csv`
- Archive payload SHA-256: `f2b9b21447c129649d1f06d927c7518c7eb14a0eaee8e159d43d541c039999d6`
- Archive manifest SHA-256: `fbb061ce92c91cfc86c1df9e07b80f07ead12d40eeb55afa2b02cbcc7e1ca513`
- Outcome artifact: `C:\\Temp\\sportsbalf-nfl-r3-results-20260911\\outcomes.csv`
- Outcome SHA-256 (from `run_manifest.json`): `b66db76d22b0088e6fd822f9d641b15e43d4bee9617db65a1c569f9e5a7fdbfc`
- Source seasons: 2021--2025; scoring: half-PPR; target: `fantasy_points`

The result manifest reports all requested seasons available, 30,750 canonical
outcomes, and the provider as immutable `nflverse-data`. The paired tournament
used only the resulting outcomes and retained strict as-of fold construction;
no test-week outcome was available to either evaluator when its prediction was
made.

## Reproduction

The cache-only outcome production command is recorded in
`R3.11-F-real-archived-baseline-run.md`. The paired comparison was then run
from the checked-in tournament adapter with the following explicit parameters
(`outcomes.csv` is the authoritative input above):

```powershell
.venv\\Scripts\\python.exe -c "import pandas as pd; from src.nfl.models.direct_fp_paired_tournament import tournament_direct_vs_internal_baseline; frame=pd.read_csv(r'C:\\Temp\\sportsbalf-nfl-r3-results-20260911\\outcomes.csv'); [print(mode, result.outer_fold_count, result.sample_count, result.mean_delta, result.ci_low, result.ci_high, result.comparison_status, result.status) for mode in ('weekly', 'season') for result in [tournament_direct_vs_internal_baseline(frame, mode=mode, seed=20260911, bootstrap_replicates=10000, confidence_level=0.95, alternative='two-sided')]]"
```

The tournament result objects were captured from that run with a deterministic
seed and 10,000 bootstrap replicates. The current adapter does not persist its
paired rows or fold results as files; therefore this packet records the
authoritative aggregate output and explicitly treats the missing persisted
paired-result artifact as an audit limitation.

## Aggregate paired results

The intervals below are two-sided 95% percentile bootstrap intervals over outer
fold deltas. Negative values favor direct FP. Both modes meet the minimum
three-fold requirement, but the status field remains `unpromoted` by contract.

| Mode | Outer folds | Paired rows | Mean delta | 95% CI | Comparison | Promotion |
| --- | ---: | ---: | ---: | --- | --- | --- |
| Weekly expanding-origin | 109 | 30,408 | -0.127437945330067 | [-0.1715501504112672, -0.0903815893655601] | `conclusive_tie_or_better` | `unpromoted` |
| Season held-out | 4 | 24,616 | +0.11562081294715854 | [0.07008600632358375, 0.15998925104816353] | `conclusive_worse` | `unpromoted` |

Weekly position slices (descriptive mean deltas only; no position-level
confidence intervals were computed in this run) were:

| Position | Mean delta |
| --- | ---: |
| QB | -0.089726 |
| RB | -0.228934 |
| TE | +0.010341 |
| WR | -0.137693 |

These position means do not establish position-level promotion or significance.

## Limitations and gate decision

This is a direct-FP versus internal-naïve comparison, not the complete R3
benchmark. Component projection candidates, component-level metrics,
probabilistic calibration, and ensemble comparisons are absent from the run.
Historical Underdog and consensus projection archives were unavailable; current
slate projections cannot be substituted for retrospective baselines. The
season-held-out direct-FP result is conclusively worse than the internal
player-trailing baseline, so it cannot be promoted despite the favorable weekly
result. Paired fold-level prediction files were not persisted, limiting
independent row-level auditability until the runner is repaired.

The explicit decision is **R4 blocked**: the seasonal comparison fails the
promotion gate, and component plus external Underdog/consensus evidence is
missing. No R4 work should begin from this evidence.
