# R3.9 Archived Backtest Audit

Status: Blocked; R4 not allowed

## Audit scope

This is a read-only audit of the current checkout against the R3.9 archived
end-to-end backtest gate. No raw data was downloaded or written, and no
production code, configuration, tests, or protected directories were changed.

## Confirmed local facts

The accepted direct dependencies are:

- R2.4 requires archived projection bytes and digest-backed provenance; its
  accepted closeout is recorded in `evidence/R2.4-scorer-repair-loop-1.md`.
- R2.5 supplies timestamp-gated as-of feature construction and rejects future
  evidence; its accepted closeout is recorded in `evidence/R2.5-asof-feature-store.md`.
- R3.1–R3.8 document component/scoring/ensemble contracts, including strict
  rolling-origin behavior and the minimum of three distinct outer rows in
  R3.8. R3.9 additionally requires an explicitly separate internal naïve
  historical/trailing-mean baseline: per-player prior observed fantasy-point
  outcomes only, with a fixed window/minimum-history/fallback policy, scored
  in every outer fold alongside components, direct-FP, Underdog, consensus,
  and ensembles.
- `config/fantasy/nfl_projection_sources_2026.yaml` points at
  `tests/testdata/fantasy/nfl/projections/nflverse_week01.csv` and
  `consensus_week01.csv`. Each is a 317-byte, two-row fixture for one player
  with calendar values 2026 Week 0 and Week 1. Their configured SHA-256
  values match the local bytes:
  `3cc634e802d868801a77b6cc94fa2b72a5a19191fbe39b168328fe0d66707e59` and
  `8bd8020c36edb7298b1d61a2c56bf0ef389ded1e73c585cdb393d557af1be0c1`.

These fixtures prove loader/scorer contracts only. They do not provide
multiple seasons, three outer weeks, a complete player slate, or an
ingestion-to-ranking historical run.

## Input availability audit

| Requirement | Local status | Evidence / gap |
|---|---|---|
| Actual outcomes plus internal historical features | Partial, insufficient | The checkout has small `tests/testdata/nflreadpy_*.csv` provider fixtures and unrelated ignored/local NFL data artifacts, but no checked-in, immutable multi-season fantasy slate archive with the required as-of manifests and complete component inputs. |
| Historical Underdog projections | Missing | The only NFL Underdog fixture found is `tests/testdata/fantasy/nfl/ud_week2_royale_2025.yaml`, a contest/line contract fixture; it is not a historical weekly projection archive aligned to outcomes and as-of cutoffs. |
| Independent consensus projections | Partial, insufficient | `consensus_week01.csv` is the two-row contract fixture; no multi-season consensus archive, source snapshots, or terms-backed historical manifest exists. |

The source config contains documentation URLs and snapshot declarations, but
the local fixture pair is the entire declared archive for this purpose. No
unverified URL, current-season feed, or synthetic rows are promoted to fill
the gap. Terms and redistribution rights for an actual historical Underdog
archive and independent consensus archive remain unverified locally.

## Required missing artifacts and acquisition boundary

Before implementation can produce a valid report, obtain approved immutable
snapshots for schedules/outcomes/features, Underdog projections, and an
independent consensus source, each with source timestamp, cutoff semantics,
identity mapping, schema/version, license/terms evidence, row count, and
SHA-256 manifest. The snapshots must cover enough aligned seasons/slates to
yield at least three distinct outer weeks after all joins and eligibility
filters. Acquisition may use an approved provider/export workflow, but it
must stop if retention or redistribution terms do not permit the intended
local evaluation. This audit does not download, scrape, or invent archives.

## Verification commands and results

Commands were run from `C:\Users\BOOSE\sportsbalf` on the current branch.

```text
git branch --show-current
codex/nfl-r3-projections
git status --short
(empty before this packet)
rg --files tests/testdata/fantasy/nfl | Sort-Object
exposure.csv; half_ppr_standard.yaml; ppr_standard.yaml;
projections/consensus_week01.csv; projections/nflverse_week01.csv;
rankings.csv; ud_week2_royale_2025.yaml
```

```text
Get-Content tests/testdata/fantasy/nfl/projections/nflverse_week01.csv
Get-Content tests/testdata/fantasy/nfl/projections/consensus_week01.csv
```

Both files contain the same header and exactly two rows for `qb-001`, with
only Week 0 and Week 1. A read-only SHA-256 check reported 317 bytes for each
file and the two digests recorded above. A tracked-file search found no
multi-season NFL projection/archive artifact beyond these fixtures; provider
code exposes network loaders, not an immutable local archive.

No end-to-end backtest was run: the required historical inputs and at least
three eligible outer weeks are absent. Consequently no metrics, confidence
intervals, internal-naïve/component/direct-FP/market-baseline comparison, or
promotion claim can be made from this checkout.

## Gate decision

`R4 blocked`. R3.9 remains incomplete until immutable, terms-approved source
snapshots and reproducible manifests exist and the full as-of rolling-origin
run reports all required metrics against internal, Underdog, and independent
consensus baselines with at least three distinct outer weeks.
