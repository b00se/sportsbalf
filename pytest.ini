[pytest]
markers =
    slow: realistic-scale or multi-model test; included in the full gate.
filterwarnings =
    ignore:np\.find_common_type is deprecated:DeprecationWarning
    ignore:In a future version.*df\.iloc.*= newvals:DeprecationWarning
